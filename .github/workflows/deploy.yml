name: Deploy to GitHub Pages

on:
  push:
    # The github-pages environment only accepts deployments from the repo's
    # default branch, which is currently claude/mebs-portfolio-site-yaog5x.
    # Once the default is switched to main, drop the second entry.
    branches: [main, claude/mebs-portfolio-site-yaog5x]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      # Requires Pages to already be enabled with source "GitHub Actions"
      # (Settings -> Pages). The workflow token is not permitted to create
      # the Pages site itself.
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
